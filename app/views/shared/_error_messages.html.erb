<% if !@deliverer.nil?%>
  <% if @deliverer.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger">
        The form contains <%= pluralize(@deliverer.errors.count, "error") %>.
      </div>

    </div>

  <% end %>
<% end %>

<% if !@shift.nil?%>
  <% if @shift.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger">
        The form contains <%= pluralize(@shift.errors.count, "error") %>.
      </div>

    </div>

  <% end %>
<% end %>
